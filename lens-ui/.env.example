# Backend base URL. Leave empty to use the Vite dev proxy (/lens, /user, /auth -> localhost:8080).
# Set this for deployed environments, e.g.:
# VITE_API_URL=https://lens-api.example.com
VITE_API_URL=
